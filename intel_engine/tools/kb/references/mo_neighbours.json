{
  "_comment": "Classification POLICY for the MO-neighbour pivot (case_state.mo_neighbour_classification, wp_mo_neighbours.discover). A co-tenant of the estate's origin IP is `same_mo` (a rung-10 related persona, never estate membership) only when its registrar is one of the estate's, its creation date falls inside the estate's created window widened by window_days, AND it either shares a naming token with the estate's own labels or its mailbox has the throwaway-handle shape. Loosening any of these names more third parties; tightening hides the deliverable. The fallback in case_state is deliberately the CONSERVATIVE minimum.",
  "classification": {
    "_comment": "window_days widens the estate's [min created, max created] window on both sides; persona_handle_regex is the throwaway-mailbox local-part shape; min_token_len / stop_tokens govern which alphabetic tokens of an apex's first label count as the estate's naming vocabulary (generic commerce words never do). Registrar match and a non-privacy e-mail/phone selector are hard requirements and not tunable here.",
    "window_days": 90,
    "persona_handle_regex": "^[a-z]+\\d{4,}$",
    "min_token_len": 4,
    "stop_tokens": [
      "www",
      "mail",
      "shop",
      "store",
      "online",
      "site",
      "web",
      "app",
      "info",
      "news",
      "home",
      "page",
      "group",
      "global",
      "official",
      "company",
      "service",
      "services",
      "vietnam",
      "saigon",
      "hanoi"
    ]
  },
  "discovery": {
    "_comment": "max_candidates = co-tenant apexes ONE origin may WHOIS-verify per run (1 DRS each); whois_run_cap = WhoisXML current-WHOIS lookups the pivot may make per RUN across all origins and collector subprocesses (stamped by run token in cases/<id>/mo_neighbours/whois_ledger.jsonl); sibling_wait_s = how long a collector subprocess waits for a sibling that is already verifying the same origin.",
    "max_candidates": 40,
    "whois_run_cap": 160,
    "whois_workers": 4,
    "sibling_wait_s": 90
  },
  "subdomains": {
    "_comment": "Frontier policy for a seed apex's OWN subdomains. Every source that yields hostnames (crt.sh SANs, Validin/SecurityTrails subdomain enumeration, urlscan hostname index, Censys cert names, IntelX phonebook, the page's own third-party links, urlscan related hosts) is mined for names under a collected apex; those are the operator's own hosts by construction (same registration) and are collected next round like the apex, then joined to it on the `apex:<registrable>` indicator (rung 1). `max_per_apex` caps the next-round batch per apex (largest-first by corroboration); `stop_labels` are hoster-generated / mail-plumbing labels (cPanel auto-DNS, autodiscover) that carry no page and are recorded as facts only; `require_dns` demands a live A/AAAA/CNAME answer before a name is seeded (CT and archives are full of dead names).",
    "max_per_apex": 12,
    "require_dns": true,
    "stop_labels": [
      "www",
      "mail",
      "webmail",
      "cpanel",
      "cpcalendars",
      "cpcontacts",
      "webdisk",
      "whm",
      "autodiscover",
      "autoconfig",
      "_dmarc",
      "_domainkey",
      "ftp",
      "ns1",
      "ns2",
      "ns3",
      "ns4",
      "mx",
      "mx1",
      "mx2",
      "smtp",
      "imap",
      "pop",
      "pop3",
      "localhost",
      "dkim",
      "_acme-challenge",
      "sitemap",
      "cdn",
      "static",
      "assets",
      "img",
      "images"
    ]
  }
}
